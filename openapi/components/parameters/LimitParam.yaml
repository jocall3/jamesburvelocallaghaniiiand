name: limit
in: query
description: The maximum number of items to return in a single page of results. Used for pagination.
required: false
schema:
  type: integer
  format: int32
  minimum: 1
  maximum: 100
  default: 20
example: 50